{
  "name": "modern-javascript-guide",
  "version": "1.0.0",
  "description": "Guide to ES6+, async/await, modules, and TypeScript",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}